meta {
  name: OutgoingCallerIds outgoing_caller_ids.delete
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/2010-04-01/Accounts/:AccountSid/OutgoingCallerIds/:Sid.json
  body: none
  auth: inherit
}

params:path {
  AccountSid: {{accountSid}}
  Sid: <Sid>
}

docs {
**Delete an OutgoingCallerId**

Returns **204 No Content** on success — no response body.

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `outgoing_caller_ids.delete`
}
