meta {
  name: SIP sip_domain_auth_registrations_credential_list_mappings.list
  type: http
  seq: 21
}

get {
  url: {{baseUrl}}/2010-04-01/Accounts/:AccountSid/SIP/Domains/:DomainSid/Auth/Registrations/CredentialListMappings.json
  body: none
  auth: inherit
}

params:path {
  AccountSid: {{accountSid}}
  DomainSid: <DomainSid>
}

query {
  ~Page: 
  ~PageSize: 
  ~PageToken: 
}

docs {
**List registration-auth CredentialList mappings on a SIP Domain (paged)**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `sip_domain_auth_registrations_credential_list_mappings.list`
}
