meta {
  name: SIP sip_domain_auth_calls_credential_list_mappings.list
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/2010-04-01/Accounts/:AccountSid/SIP/Domains/:DomainSid/Auth/Calls/CredentialListMappings.json
  body: none
  auth: inherit
}

params:path {
  AccountSid: {{accountSid}}
  DomainSid: <DomainSid>
}

query {
  ~Page: 
  ~PageSize: 
  ~PageToken: 
}

docs {
**List call-auth CredentialList mappings on a SIP Domain (paged)**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `sip_domain_auth_calls_credential_list_mappings.list`
}
