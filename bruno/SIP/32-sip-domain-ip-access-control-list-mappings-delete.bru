meta {
  name: SIP sip_domain_ip_access_control_list_mappings.delete
  type: http
  seq: 32
}

delete {
  url: {{baseUrl}}/2010-04-01/Accounts/:AccountSid/SIP/Domains/:DomainSid/IpAccessControlListMappings/:Sid.json
  body: none
  auth: inherit
}

params:path {
  AccountSid: {{accountSid}}
  DomainSid: <DomainSid>
  Sid: <Sid>
}

docs {
**Unmap an IpAccessControlList from a SIP Domain**

Returns **204 No Content** on success — no response body.

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `sip_domain_ip_access_control_list_mappings.delete`
}
