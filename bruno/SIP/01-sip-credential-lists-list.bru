meta {
  name: SIP sip_credential_lists.list
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/2010-04-01/Accounts/:AccountSid/SIP/CredentialLists.json
  body: none
  auth: inherit
}

params:path {
  AccountSid: {{accountSid}}
}

query {
  ~FriendlyName: 
  ~Page: 
  ~PageSize: 
  ~PageToken: 
}

docs {
**List SIP CredentialLists for the account (paged)**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `sip_credential_lists.list`
}
