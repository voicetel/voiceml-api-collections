meta {
  name: Assistants v1 assistants.v1.tools.list
  type: http
  seq: 26
}

get {
  url: {{baseUrl}}/v1/Tools
  body: none
  auth: inherit
}

query {
  ~AssistantId: 
  ~PageSize: 
}

docs {
**List Tools**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `assistants.v1.tools.list`
}
