meta {
  name: Assistants v1 assistants.v1.tools.delete
  type: http
  seq: 30
}

delete {
  url: {{baseUrl}}/v1/Tools/:id
  body: none
  auth: inherit
}

params:path {
  id: <id>
}

docs {
**Delete a Tool**

Returns **204 No Content** on success — no response body.

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `assistants.v1.tools.delete`
}
