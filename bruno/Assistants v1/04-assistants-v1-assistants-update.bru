meta {
  name: Assistants v1 assistants.v1.assistants.update
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/v1/Assistants/:id
  body: json
  auth: inherit
}

params:path {
  id: <id>
}

headers {
  Content-Type: application/json
}

body:json {
{}
}

docs {
**Update an Assistant**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `assistants.v1.assistants.update`
}
