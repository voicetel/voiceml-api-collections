meta {
  name: Assistants v1 assistants.v1.assistants.fetch
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/Assistants/:id
  body: none
  auth: inherit
}

params:path {
  id: <id>
}

docs {
**Fetch an Assistant (with tools and knowledge)**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `assistants.v1.assistants.fetch`
}
