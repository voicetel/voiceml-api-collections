meta {
  name: Assistants v1 assistants.v1.knowledge.status.fetch
  type: http
  seq: 21
}

get {
  url: {{baseUrl}}/v1/Knowledge/:id/Status
  body: none
  auth: inherit
}

params:path {
  id: <id>
}

docs {
**Fetch Knowledge ingestion status**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `assistants.v1.knowledge.status.fetch`
}
