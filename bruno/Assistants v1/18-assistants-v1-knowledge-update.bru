meta {
  name: Assistants v1 assistants.v1.knowledge.update
  type: http
  seq: 18
}

put {
  url: {{baseUrl}}/v1/Knowledge/:id
  body: json
  auth: inherit
}

params:path {
  id: <id>
}

headers {
  Content-Type: application/json
}

body:json {
{}
}

docs {
**Update Knowledge**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `assistants.v1.knowledge.update`
}
