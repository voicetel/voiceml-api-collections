meta {
  name: Diagnostics · OpenAPI spec (YML alias)
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/openapi.yml
  body: none
  auth: none
}

docs {
**OpenAPI spec (YAML, .yml alias)**
Alias for `/openapi.yaml`. Same response, same content type.

No authentication required.

OperationId: `openapi.yml`
}
