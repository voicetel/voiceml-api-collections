meta {
  name: Conversation Services v1 conversations.v1.services.configuration.fetch
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/Services/:ChatServiceSid/Configuration
  body: none
  auth: inherit
}

params:path {
  ChatServiceSid: <ChatServiceSid>
}

docs {
**Fetch the per-service Configuration**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.services.configuration.fetch`
}
