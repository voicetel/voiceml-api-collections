meta {
  name: Conversation Services v1 conversations.v1.services.configuration.update
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/v1/Services/:ChatServiceSid/Configuration
  body: json
  auth: inherit
}

params:path {
  ChatServiceSid: <ChatServiceSid>
}

headers {
  Content-Type: application/json
}

body:json {
{}
}

docs {
**Update the per-service Configuration**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.services.configuration.update`
}
