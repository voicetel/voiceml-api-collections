meta {
  name: Conversation Services v1 conversations.v1.services.roles.delete
  type: http
  seq: 38
}

delete {
  url: {{baseUrl}}/v1/Services/:ChatServiceSid/Roles/:Sid
  body: none
  auth: inherit
}

params:path {
  ChatServiceSid: <ChatServiceSid>
  Sid: <Sid>
}

docs {
**Delete a service-scoped Role**

Returns **204 No Content** on success — no response body.

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.services.roles.delete`
}
