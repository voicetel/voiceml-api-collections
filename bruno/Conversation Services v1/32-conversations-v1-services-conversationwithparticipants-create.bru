meta {
  name: Conversation Services v1 conversations.v1.services.conversationWithParticipants.create
  type: http
  seq: 32
}

post {
  url: {{baseUrl}}/v1/Services/:ChatServiceSid/ConversationWithParticipants
  body: json
  auth: inherit
}

params:path {
  ChatServiceSid: <ChatServiceSid>
}

headers {
  Content-Type: application/json
}

body:json {
{}
}

docs {
**Create a service-scoped Conversation with initial Participants**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.services.conversationWithParticipants.create`
}
