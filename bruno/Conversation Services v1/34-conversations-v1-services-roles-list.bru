meta {
  name: Conversation Services v1 conversations.v1.services.roles.list
  type: http
  seq: 34
}

get {
  url: {{baseUrl}}/v1/Services/:ChatServiceSid/Roles
  body: none
  auth: inherit
}

params:path {
  ChatServiceSid: <ChatServiceSid>
}

query {
  ~PageSize: 
}

docs {
**List service-scoped Roles**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.services.roles.list`
}
