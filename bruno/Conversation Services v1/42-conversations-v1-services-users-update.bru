meta {
  name: Conversation Services v1 conversations.v1.services.users.update
  type: http
  seq: 42
}

post {
  url: {{baseUrl}}/v1/Services/:ChatServiceSid/Users/:Sid
  body: json
  auth: inherit
}

params:path {
  ChatServiceSid: <ChatServiceSid>
  Sid: <Sid>
}

headers {
  Content-Type: application/json
}

body:json {
{}
}

docs {
**Update a service-scoped User**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.services.users.update`
}
