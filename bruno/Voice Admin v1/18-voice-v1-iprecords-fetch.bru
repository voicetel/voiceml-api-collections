meta {
  name: Voice Admin v1 voice.v1.ipRecords.fetch
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/v1/IpRecords/:Sid
  body: none
  auth: inherit
}

params:path {
  Sid: <Sid>
}

docs {
**Fetch an IpRecord**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `voice.v1.ipRecords.fetch`
}
