meta {
  name: Voice Admin v1 voice.v1.byocTrunks.update
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/v1/ByocTrunks/:Sid
  body: json
  auth: inherit
}

params:path {
  Sid: <Sid>
}

headers {
  Content-Type: application/json
}

body:json {
{}
}

docs {
**Update a ByocTrunk**
Updates a ByocTrunk. Only set fields are changed.

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `voice.v1.byocTrunks.update`
}
