meta {
  name: Voice Admin v1 voice.v1.ipRecords.list
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/v1/IpRecords
  body: none
  auth: inherit
}

query {
  ~PageSize: 
}

docs {
**List IpRecords**
Paginated list of the authenticated account's IpRecords.

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `voice.v1.ipRecords.list`
}
