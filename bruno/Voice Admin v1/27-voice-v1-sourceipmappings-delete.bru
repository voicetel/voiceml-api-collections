meta {
  name: Voice Admin v1 voice.v1.sourceIpMappings.delete
  type: http
  seq: 27
}

delete {
  url: {{baseUrl}}/v1/SourceIpMappings/:Sid
  body: none
  auth: inherit
}

params:path {
  Sid: <Sid>
}

docs {
**Delete a SourceIpMapping**

Returns **204 No Content** on success — no response body.

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `voice.v1.sourceIpMappings.delete`
}
