meta {
  name: Conversations v1 conversations.v1.credentials.update
  type: http
  seq: 36
}

post {
  url: {{baseUrl}}/v1/Credentials/:Sid
  body: json
  auth: inherit
}

params:path {
  Sid: <Sid>
}

headers {
  Content-Type: application/json
}

body:json {
{}
}

docs {
**Update a push Credential**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.credentials.update`
}
