meta {
  name: Conversations v1 conversations.v1.credentials.list
  type: http
  seq: 33
}

get {
  url: {{baseUrl}}/v1/Credentials
  body: none
  auth: inherit
}

query {
  ~PageSize: 
}

docs {
**List push Credentials**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.credentials.list`
}
