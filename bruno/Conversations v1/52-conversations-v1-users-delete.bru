meta {
  name: Conversations v1 conversations.v1.users.delete
  type: http
  seq: 52
}

delete {
  url: {{baseUrl}}/v1/Users/:Sid
  body: none
  auth: inherit
}

params:path {
  Sid: <Sid>
}

docs {
**Delete a User**

Returns **204 No Content** on success — no response body.

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.users.delete`
}
