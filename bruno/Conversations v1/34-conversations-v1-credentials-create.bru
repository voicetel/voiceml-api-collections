meta {
  name: Conversations v1 conversations.v1.credentials.create
  type: http
  seq: 34
}

post {
  url: {{baseUrl}}/v1/Credentials
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
{
  "Type": "apn"
}
}

docs {
**Create a push Credential**

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.credentials.create`
}
