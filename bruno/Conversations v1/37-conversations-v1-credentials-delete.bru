meta {
  name: Conversations v1 conversations.v1.credentials.delete
  type: http
  seq: 37
}

delete {
  url: {{baseUrl}}/v1/Credentials/:Sid
  body: none
  auth: inherit
}

params:path {
  Sid: <Sid>
}

docs {
**Delete a push Credential**

Returns **204 No Content** on success — no response body.

HTTP Basic Auth: username = `{{accountSid}}`, password = `{{apiKey}}`.

OperationId: `conversations.v1.credentials.delete`
}
